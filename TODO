- Create a hosted service which will allow to submit the data for invoices
  -- Deploy the invoice generating service as a lambda to AWS, it should generate the invoice and store it in S3, should reply with the generated pdf
  -- Add a frontend
    -- Gmail authentication 
    -- Allow to submit the data for invoices and generate them using the lambda
    -- Allow to download the generated invoices
- Define the template for the invoice number, created invoices can then be downloaded from the Web interface
